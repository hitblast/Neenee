[tool.poetry]
name = "neenee"
version = "0.0.1"
description = "My personal Discord bot aimed at doing chores and performing as a development ground for me"
authors = ["HitBlast <hitblastlive@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
